import type { GuideContent } from "../types";

/**
 * Extended, descriptive editorial for each developer (history, reputation,
 * signature projects, buyer guidance), overlaid onto the developer by slug in
 * the repository. Generated content, refine any time.
 */
export const developerContent: Record<string, GuideContent> = {};
