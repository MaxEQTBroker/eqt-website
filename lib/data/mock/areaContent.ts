import type { GuideContent } from "../types";

/**
 * Extended, descriptive editorial for each community, overlaid onto the area by
 * slug in the repository. Keeps the core area records lean; this is the "what
 * it's like to live/invest here" depth. Generated content, refine any time.
 */
export const areaContent: Record<string, GuideContent> = {};
